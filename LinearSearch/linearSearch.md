
# Linear Search 
```
1. Linear search is an aloright to search the element in the given array in the linear fashion. 
2. it return the index of the element if it found other wise -1 will be returned 

Algoright --> 

a-> find the lenght of the array.lenght;
b -> Iterate over the array  
     * check if (arr[x] == element) 
              * idx = x;
              * break; //stop the loop 
     * if (idx != -1) return idx
c -- >return -1;

```